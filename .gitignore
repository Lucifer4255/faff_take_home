# deps
node_modules/

# next.js
.next/
out/
next-env.d.ts

# build
dist/
tsconfig.tsbuildinfo

# env & secrets — never commit
.env
.env*.local

# mastra storage + mock/runtime state — out of repo
.data/
*.db
*.db-*
.mastra/
mastra.db*
scratchpad/
# playwright captured auth / browser state — never commit
.playwright/
*.storageState.json

# os
.DS_Store
